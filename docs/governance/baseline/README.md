# Baseline

The frozen text of the standard at each major version. Files here are never edited in place;
`BASELINE.sha256` records their hashes and the gate verifies them (E-3.3). This README is not
part of the baseline. The baseline of 1.0.0 is written when the standard's documents are complete
and reviewed by the owner.
