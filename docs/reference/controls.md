# Controls

Every control enforces the clause of the same identifier in [the standard](../standard/STANDARD.md).
`estatuto gate` runs the controls that apply to the repository's role. A repository passes when
every applicable control passes or is validly waived; there is no averaging.

::: estatuto.gate.controls
    options:
      members: true
      show_root_heading: false
