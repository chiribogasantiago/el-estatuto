# Baseline

The frozen design description of __NAME__. Files here are never edited in place: a new version is
a new file, and `BASELINE.sha256` records the hash of every frozen file. The standard gate
verifies the hashes. This README is not part of the baseline.
